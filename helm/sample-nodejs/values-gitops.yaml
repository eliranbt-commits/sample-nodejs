# Overlay used by ArgoCD. CI overwrites repository and tag after a successful
# image scan and Docker Hub push. Local kind still uses values.yaml defaults.
image:
  repository: docker.io/CHANGE_ME/sample-nodejs
  tag: "1.0.0"
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: dockerhub
